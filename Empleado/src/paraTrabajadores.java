public interface paraTrabajadores {

    // Se sobreentienden "public" y "abstract" al ser una interfaz
    double setBonus(double gratificacion);

    // Se sobreentienden por ser interfaz "public final static"
    double bonus=200;
}
